This question is basically asking us to implement Stack from scrach. We can do the solution either by using 1 stack class from library or using a modified Linked List.

class MinStack {
    Stack<Integer> st=new Stack<>();
    int min=Integer.MAX_VALUE;
    public MinStack() {
        
    }
    
    public void push(int val) {
        st.push(val);
        if(x<min){
            min=x;
        }
    }
    
    public void pop() {
        if(st.peek()==min){
            
        }
    }
    
    public int top() {
        
    }
    
    public int getMin() {
        
    }
}

/**
 * Your MinStack object will be instantiated and called as such:
 * MinStack obj = new MinStack();
 * obj.push(val);
 * obj.pop();
 * int param_3 = obj.top();
 * int param_4 = obj.getMin();
 */
